{% extends 'operatorQuarterly/_layout.njk' %}

{% block stepBody %}
  <form method="post" action="{{ action }}" novalidate data-testid="operator-quarterly-form">
    {{ govukCheckboxes({
      idPrefix: "declarationAccepted",
      name: "declarationAccepted",
      errorMessage: { text: errors.declarationAccepted } if errors.declarationAccepted,
      items: [
        {
          value: "yes",
          text: labels.declarationLabel,
          checked: formValues.declarationAccepted == "yes"
        }
      ],
      attributes: { "data-testid": "operator-quarterly-declaration" }
    }) }}

    {{ govukButton({
      text: confirmAction,
      attributes: { "data-testid": "operator-quarterly-confirm" }
    }) }}
  </form>
{% endblock %}
