import { dashboard } from './dashboard/index.js'

// compare, accept, accepted, query and querySent are added as the rest of
// this journey is built.
export const prototypeAbtoIncomingWaste = {
  openRoutes: [...dashboard.openRoutes]
}
