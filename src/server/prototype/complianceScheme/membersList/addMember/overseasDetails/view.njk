{% extends 'prototypeMembersListAddMember/_layout.njk' %}
{% from "prototypeMembersListAddMember/_address-fields.njk" import addressFields %}

{% block stepBody %}
  <form method="post" action="{{ action }}" novalidate data-testid="overseas-details-form">
    {{ govukInput({
      id: "overseasName",
      name: "overseasName",
      value: formValues.overseasName,
      label: { text: labels.overseasNameLabel, classes: "govuk-label--m" },
      errorMessage: { text: errors.overseasName } if errors.overseasName,
      classes: "govuk-!-width-two-thirds",
      attributes: { "data-testid": "overseas-name" }
    }) }}

    {{ govukInput({
      id: "overseasAddress",
      name: "overseasAddress",
      value: formValues.overseasAddress,
      label: { text: labels.overseasAddressLabel, classes: "govuk-label--m" },
      errorMessage: { text: errors.overseasAddress } if errors.overseasAddress,
      classes: "govuk-!-width-two-thirds",
      attributes: { "data-testid": "overseas-address" }
    }) }}

    {{ addressFields({
      addressHeading: labels.ukPresenceHeading,
      postcodeLabel: labels.postcodeLabel,
      postcodeHint: labels.postcodeHint,
      findAddressAction: labels.findAddressAction,
      selectAddressLabel: labels.selectAddressLabel,
      manualLink: labels.manualLink,
      addressLine1Label: labels.addressLine1Label,
      addressTownLabel: labels.addressTownLabel
    }, errors, formValues) }}

    {{ govukButton({
      text: labels.continueAction,
      attributes: { "data-testid": "details-continue" }
    }) }}
  </form>
{% endblock %}
