{% extends 'complianceSchemeIa/_layout.njk' %}

{% from "govuk/components/panel/macro.njk" import govukPanel %}

{% block stepBody %}
  {{ govukPanel({
    titleText: labels.heading,
    text: labels.intro,
    attributes: { "data-testid": "ia-confirmation-panel" }
  }) }}

  <p class="govuk-body">
    <a class="govuk-link" href="{{ dashboardUrl }}" data-testid="ia-confirmation-dashboard-link">{{ labels.returnToDashboardAction }}</a>
  </p>
{% endblock %}
